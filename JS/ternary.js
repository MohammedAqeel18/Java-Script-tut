//ternary is a function that takes a condition and two values, and returns one of the values based on the condition
let subscriber = false;

let response = subscriber ? "thank you for your subscription" : "kindly subscribe for intersting contents";

console.log(response);


let testScore= 84;

let grade = testScore>89 ? "A":  testScore> 80? "B": testScore> 70? "C": testScore>51? "D+" : "F";

console.log(grade);

